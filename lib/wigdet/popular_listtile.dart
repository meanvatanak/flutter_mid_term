import 'package:flutter/material.dart';

class PopularListTile extends StatelessWidget {
  const PopularListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return ListTile(

    );
  }
}
